<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Default state -->
    <item android:drawable="@color/black" android:state_pressed="false" />
    <!-- Pressed state -->
    <item android:drawable="@color/gold" android:state_pressed="true" />
</selector>